# Devices externos

Destino lógico de arquivos `.lsdevice` adicionados pelo usuário. Em execução, a extensão mantém a
mesma estrutura sob seu `globalStorageUri`, para não escrever dentro da instalação do VS Code.
